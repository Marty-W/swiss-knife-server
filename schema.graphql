### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  token: String!
  user: User!
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

type Mutation {
  changeTaskTitle(newTitle: String!, taskId: String!): Task
  createTask(tags: [String!], title: String!): Task
  markTaskDone(taskId: String!): Task
  signin(email: String!, password: String!): AuthPayload
  signup(email: String!, password: String!): AuthPayload
  undoTask(taskId: String!): Task
}

type PomodoroSession {
  actualDuration: Int!
  assumedDuration: Int!

  """Id of the user that created the session"""
  byUserId: String!

  """When the session should end,  in ISO format"""
  endTime: DateTime

  """Id of the pomodoro session"""
  id: String!

  """When the pomodoro started, in ISO format"""
  startTime: DateTime!
}

type Query {
  allTasks: [Task!]
  user(userId: String!): User
}

type Tag {
  id: String!
  title: String!
}

type Task {
  """Id of the User that created the task"""
  byUserId: String!

  """True if the task has been completed"""
  completed: Boolean!

  """User that created the task"""
  createdBy: User

  """When the task is due, in ISO format."""
  createdOn: DateTime!

  """ID of the task"""
  id: String!
  tags: [Tag]

  """Text of the task"""
  title: String!
}

type User {
  email: String!
  id: String!
  password: String!
  pomoSessions: [[PomodoroSession]]
  tasks: [Task!]
}